import EditableTable from '@/components/ui/editable-table/EditableTable';
import { FC, Suspense } from 'react';

const Home: FC = () => {
  return (
    // <Suspense>Home</Suspense>
    <EditableTable />
  );
};

export default Home;
